See `docs/AGENTS_UNIVERSAL.md` for core instructions.

## GPT Specific Instructions
*   Focus on robust architectural decisions when expanding `server_player` or `client_stats`.
*   Ensure all new feature branches are cleanly separated by concern (e.g., UI vs Physics).
*   Always run `make` after every C file modification before committing.